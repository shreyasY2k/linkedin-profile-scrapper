---
topic: LinkedIn Profile API — hosted HTTPS service returning a LinkedIn profile as structured JSON
updated: 2026-08-27T12:13
---

- (event) Create from pre-sorted upstream input: brief.md + addendum.md at _bmad-output/planning-artifacts/briefs/brief-linkedin-profile-scraper-2026-08-27/. Authored separation trusted; no elicitation.
- (direction) Slug inherited from source folder: linkedin-profile-scraper.
- (capability) CAP-1 Profile retrieval: caller submits a LinkedIn profile URL, receives the profile as structured JSON across the assignment field list.
- (capability) CAP-2 Caller authentication: requests without a valid Keycloak-issued token are rejected; no unauthenticated access to profile data.
- (capability) CAP-3 Scriptable evaluator access: Keycloak service-account client_credentials grant gives a token in one curl, with no browser redirect.
- (capability) CAP-4 Per-user LinkedIn session: an authenticated user supplies their own li_at cookie, stored encrypted and bound to their Keycloak subject; requests use the caller's own session.
- (capability) CAP-5 Stale-serve: successful fetches are cached; when the live call fails the last good record is returned flagged stale with its fetch timestamp, rather than an error.
- (capability) CAP-6 Structured failures: every error path returns a typed error body with a correct status code; no unhandled 500s.
- (capability) CAP-7 Reproducible local run: a cold clone plus documented steps yields a working local instance behaviourally identical to the deployed one.
- (capability) CAP-8 Submission documentation: README covers setup, API documentation, approach, and known limitations - all four required by the assignment.
- (constraint) Retrieval goes through LinkedIn internal Voyager JSON endpoints with an authenticated session, not rendered-HTML parsing. This is the assignment's core 'reverse engineer LinkedIn APIs' requirement and rules out the logged-out HTML approach.
- (constraint) Twelve-factor config: all settings via env vars, .env.example committed with dummy values, real .env gitignored, same image runs local and deployed. Rules out hardcoded or environment-branched config.
- (constraint) No credential, cookie, key or secret in the repository or its git history. Verified against history, not just the working tree.
- (constraint) Hosting is Oracle Cloud Always Free ARM Ampere A1 behind Cloudflare on shreyaskaushik.dpdns.org. Alternate hosts were offered and declined; accepted single point of failure.
- (constraint) Stack footprint is ~2GB+, so the Always Free AMD 1GB micro shape is not viable - the ARM Ampere shape is required, not preferred.
- (constraint) li_at cookies encrypted at rest with a key supplied via env and never committed.
- (constraint) Python 3.11+ / FastAPI / Pydantic. Generated OpenAPI document serves as the README's API documentation.
- (constraint) Submission deadline 2026-08-29. Under time pressure, scope is cut in the order fixed in scope-tiers.md; the Must tier is never cut.
- (decision) Capabilities carry the Must tier only. Should and Could tiers (Google SSO federation, rate limiting, Playwright fallback) live in the scope-tiers.md companion so the kernel is not read as committing to them.
- (decision) Companions: response-schema.md and scope-tiers.md spec-authored; the upstream addendum.md adopted by relative path rather than duplicated. brief.md listed under sources as fully absorbed.
- (decision) Playwright headless-browser fallback kept out of Capabilities and out of Non-goals: it is a Could-tier stretch item, recorded in scope-tiers.md.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 and 8): PASS. All 8 capabilities carry intent and success. Intents kept to WHAT - retrieval mechanism moved to Constraints rather than stated as a capability. All 9 constraints rule something out. 6 non-goals. Success signal testable. CAP-1..8 unique, first issue.
- (event) Self-validate pass 2 (preservation): PASS after one repair. Gap found - the day-1/day-2 delivery sequencing existed only in brief.md, which frontmatter lists under sources and downstream does not read. Repaired by adding a Delivery sequence section to scope-tiers.md.
- (note) Wrapper-only content dropped: brief's competitive framing (PhantomBuster/Proxycurl pricing, manual-copy alternatives) and the Vision section. Narrative rationale with no downstream decision attached; remains in brief.md for traceability.
- (decision) Absent-versus-unreadable semantics fixed in response-schema.md: genuinely-absent fields are null or empty array, unreadable fields are omitted and named in a top-level partial[] array. Prevents a consumer mistaking extraction failure for an empty profile.
- (decision) Stale-serve takes precedence over retryable errors: RATE_LIMITED, UPSTREAM_CHALLENGE and UPSTREAM_ERROR return 200 with stale:true when a cached record exists; the typed error surfaces only when nothing is cached.
- (decision) 428 chosen over 403 for NO_SESSION and SESSION_EXPIRED - the condition is a fixable missing precondition, which is what the caller needs to act on.
- (decision) Story Breakdown run interactively. 9 stories accepted as proposed.
- (decision) Deployment moved from last story to story 2 as a walking skeleton (/health through Oracle + Cloudflare + DNS + TLS). Surfaces capacity, ingress, cert and DNS-propagation failures on day 1; every later story is then a redeploy.
- (decision) Story 2 infra gate resolved by user: do NOT block on ARM capacity. Write and locally validate the deployment assets so applying them is one command once an instance exists.
- (decision) Checkpoints set by user: done_checkpoint on stories 2, 6, 9; spec_checkpoint on story 5 (encrypted session vault, security-sensitive design reviewed before implementation).
- (event) stories.yaml validity check: PASS. 9 entries, ids 1-9 unique quoted strings, no status field, no prefix collisions, all required fields present. Verified programmatically against assets/stories-schema.md rules 1-4.
- (decision) OPEN QUESTION 2 RESOLVED: session overwrite is sufficient. PUT /api/v1/session replaces any stored li_at; no DELETE or revoke path in the Must tier.
- (decision) OPEN QUESTION 1 RESOLVED: stale-serve is unbounded. Cached records never expire and have no TTL; a cached profile of any age is served with stale:true rather than an error. fetched_at is the caller's sole staleness signal.
- (event) ASSUMPTION RESOLVED TO FACT: the Oracle instance is already created and running. The ARM-capacity risk that dominated the brief is retired, and open question 3 (which region and ADs for the retry loop) is moot.
- (constraint) Deployment topology fixed: Cloudflare DNS resolves to an OCI Load Balancer, which forwards to the instance, where host-installed nginx reverse-proxies into the Docker Compose stack. Rules out binding application containers directly to public ports.
- (constraint) The entire application stack - API, Keycloak, Postgres - comes up under a single docker compose. nginx and Docker are host-installed and are the only components configured outside compose, which makes nginx the sole deployment-time wiring step.
- (question) NEW OPEN QUESTION: where does TLS terminate - Cloudflare, the OCI load balancer, or nginx? Determines the nginx config, whether a certificate is needed on the LB, and whether Let's Encrypt renewal is in scope. Recommendation: Cloudflare proxied with a Cloudflare Origin Certificate installed at nginx, which is free, 15-year, and removes renewal entirely.
- (event) Re-derived SPEC.md, response-schema.md and scope-tiers.md from memlog after the infra and open-question updates. stories.yaml deliberately not rewritten - story 2's dispatch note still tells the dev skill not to block on ARM capacity, which is now obsolete. Mismatch surfaced to user with an offer to re-run Story Breakdown.
- (decision) Story Breakdown re-run. Story 2 rewritten for the real topology: instance is live, so deploy for real - OCI load balancer wiring, host nginx reverse proxy, host firewall alongside the security list, TLS termination per the open question. No longer a write-scripts-and-wait story.
- (decision) Story 7 dispatch note updated with the unbounded-cache decision: no TTL, no eviction.
- (decision) Story 5 dispatch note updated: PUT overwrites outright, no delete endpoint in the Must tier.
- (note) Story ids 1-9 unchanged and unpinned (no stories/ spec files exist yet); ordering and slice boundaries unchanged.
- (event) stories.yaml re-derived and re-validated: 9 entries, ids 1-9 unchanged, all schema validity rules pass, checkpoints preserved (done on 2/6/9, spec on 5).
